<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>User Scores</title>
    <style>
      .user-block {
        border: 1px solid black;
        border-radius: 10px;
        margin-bottom: 10px;
        padding: 10px;
        display: flex;
        align-items: center;
        margin-right: 16px;
      }

      .user-number,
      .user-name,
      .user-score {
        width: 33%;
      }

      .records {
        overflow-y: auto;
      }

      h1,
      .user-block {
        -webkit-touch-callout: none; /* iOS Safari */
        -webkit-user-select: none; /* Safari */
        -khtml-user-select: none; /* Konqueror HTML */
        -moz-user-select: none; /* Firefox */
        -ms-user-select: none; /* Internet Explorer/Edge */
        user-select: none; /* Non-prefixed version, currently supported by Chrome and Opera */
      }

      html,
      body {
        width: 100%;
        margin: 0;
        padding: 0;
        border: 0;
      }

      body {
        height: 100%;
        background-image: url("/public/background.png");
        background-size: cover;
        background-repeat: no-repeat;
        background-attachment: fixed;
      }

      h1 {
        text-align: center;
        color: rgb(46, 46, 46);
      }

      #ranks {
        width: 80%;
        height: 85vh;
        margin: auto;
        margin-top: 4vh;
        padding: 0 12px 12px 12px;
        border-radius: 10px;
        background-color: white;
      }

      #head {
        color: whitesmoke;
        background-color: rgb(46, 46, 46);
      }

      #records {
        height: 80%;
        overflow-y: auto;
      }
    </style>
  </head>
  <body>
    <div id="ranks">
      <h1>Leader Board</h1>

      <div id="records">
        <div id="head" class="user-block">
          <div class="user-number">Rank</div>
          <div class="user-name">Name</div>
          <div class="user-score">Score</div>
        </div>
        <% if(records.length > 0) { %> <% records.forEach(function(user, index)
        { %>
        <div class="user-block">
          <div class="user-number"><b>#<%= index + 1 %></b></div>
          <div class="user-name"><%= user.user_name %></div>
          <div class="user-score"><%= user.score %></div>
        </div>
        <% }) %> <% } else { %>
        <p>No user scores available.</p>
        <% } %>
      </div>
    </div>
  </body>
</html>
